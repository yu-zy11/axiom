## ADR: <标题>

- **状态**：提议 / 已接受 / 已废弃
- **日期**：YYYY-MM-DD
- **影响模块**：core / diag / math / geo / topo / rep / io / ops / heal / eval / plugin / sdk / tests

### 背景与问题

- **现状**：
- **痛点**：
- **约束**（性能/鲁棒/容差/可观测/兼容性/可回归）：

### 决策

一句话结论：

### 方案对比

- **方案 A（默认）**：
  - 优点：
  - 缺点：
  - 风险：
- **方案 B（备选）**：
  - 优点：
  - 缺点：
  - 风险：

### 接口契约（必须写清）

- **输入合法性**：NaN/Inf、零向量、退化 bbox、空集合
- **容差策略**：linear/angular/scale policy 的默认值与传播范围
- **错误码/诊断码**：新增/复用哪些码，哪些路径必须绑定
- **数据结构**：owned vs source、缓存 key、版本/事务语义

### 验收标准（DoD）

- [ ] 实现主链路
- [ ] 实现失败/告警路径（含诊断）
- [ ] 完成回归测试：正常/边界/失败
- [ ] 文档更新：对齐 `docs/plan/**`

### 测试计划

- **新增/修改测试文件**：
- **关键断言**：
- **性能门槛（如有）**：

### 迁移/发布注意

- **兼容性**：
- **风险缓解**：

